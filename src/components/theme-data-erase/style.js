import styled from 'styled-components'
import { Button } from '../button'

export const DataEraseButton = styled (Button)`
    font-size: 1.5rem;
    width: 200px;
    &:hover {
        font-size: 1.6rem;
    }

`